kind: 'AppHostingYaml'
version: 'v1'

runConfig:
  maxInstances: 1

setupConfig:
  installConfig:
    # Cette commande ignore les erreurs de versions et répare ton projet
    command: npm install --legacy-peer-deps --force
