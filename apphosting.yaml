kind: 'AppHostingYaml'
version: 'v1'

runConfig:
  maxInstances: 1

setupConfig:
  installConfig:
    # On force npm install (plus souple) au lieu de npm ci (trop strict)
    command: npm install --legacy-peer-deps --force
