runConfig:
  maxInstances: 1

run:
  build:
    command: npm install --package-lock-only && npm install && npm run build
  serve:
    runtime: nodejs20

env:
  - variable: NPM_CONFIG_LEGACY_PEER_DEPS
    value: "true"
