kind: AppHostingYaml
version: v1

runConfig:
  maxInstances: 1

setupConfig:
  installConfig:
    # Use legacy-peer-deps to ignore minor version mismatches
    command: npm install --legacy-peer-deps
