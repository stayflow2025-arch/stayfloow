kind: 'AppHostingYaml'
version: 'v1'

runConfig:
  maxInstances: 1

setupConfig:
  installConfig:
    # C'EST ICI que la commande doit être pour corriger l'erreur de lockfile
    command: npm install --legacy-peer-deps --force
