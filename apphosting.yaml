kind: AppHostingYaml
version: v1

runConfig:
  maxInstances: 1

setupConfig:
  installConfig:
    # Forces 'npm install' to bypass strict lockfile synchronization errors
    command: npm install --legacy-peer-deps
